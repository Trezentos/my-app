import React, { useState } from 'react';
import Button from '../../components/Button';

const Home: React.FC = () => {
 return (
  <>
   <Button>Click</Button>
   <Button>Click</Button>
   <Button>Click</Button>
  </>
 );
};

export default Home;
